language: node_js

dist: trusty

node_js:
- "8"

before_install:
 - export DISPLAY=:99.0
 - sh -e /etc/init.d/xvfb start
 - sleep 3 # give xvfb some time to start

sudo: false

addons:
  chrome: stable

# before_script:
#   - npm install -g gulp-cli

# script: gulp